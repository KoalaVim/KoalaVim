# implements
snippet implements
	var _ ${1:interface} = (*${2:type})(nil) // Verify that *$2 implements $1.
# struct
snippet struct
	type ${1:name} struct {
		$2
	}
# print caller
snippet caller
	_, file, no, ok := runtime.Caller(1)
	if ok {
		fmt.Printf("called from %s#%d\n", file, no)
	}
