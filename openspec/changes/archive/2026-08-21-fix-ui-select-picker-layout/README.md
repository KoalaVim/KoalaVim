# fix-ui-select-picker-layout

Restore snacks picker preset resolution so vim.ui.select pickers (sidekick agent picker, etc.) use the compact no-preview select layout instead of the full-size picker with a vim.inspect dump in the preview pane
